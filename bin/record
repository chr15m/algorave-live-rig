#!/bin/sh

if [ "$1" = "--stop" ]
then
  ./bin/send recorder/record stop
else
  filename="`date +"%Y-%m-%d-%H:%M"`-algorave-live-set.wav"
  ./bin/send recorder/record open "${filename}"
  ./bin/send recorder/record start
  echo $filename
fi
